import styled from 'styled-components';

export const DjRadiosListWrapper = styled.div`
  .DjRadios-list {
    display: inline-flex;
    flex-wrap: wrap;
    justify-content: space-between;
    margin-right: -30px;
  }
`