import styled from 'styled-components'

export const XZXSingerWrapper = styled.div`
  display: inline-flex;
  flex-wrap: wrap;
  margin-top: 20px;
  .page{
    position:relative;
    margin:0 auto;
  }
`
